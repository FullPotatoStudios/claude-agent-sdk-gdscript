# Tests

This directory contains development-only tests for the project.

These tests are not part of the future distributable addon payload.

Current split:

- `scaffolding/`

Planned later:

- parser/model tests
- transport/command-building tests
- control-routing tests
