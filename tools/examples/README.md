# Examples

These are lightweight development examples for the adapter layer.

- `adapter_usage_example.gd` shows a signal-driven `RefCounted` integration with `ClaudeClientAdapter`
- `node_usage_example.gd` shows the same idea through `ClaudeClientNode`

They are not the reusable chat panel and they are not part of the distributable addon payload.
