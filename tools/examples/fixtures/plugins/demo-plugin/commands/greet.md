# Greet Command

This is a custom greeting command from the local GDScript SDK plugin fixture.

When the user runs this command, greet them warmly and mention that the command
came from a plugin loaded through `ClaudeAgentOptions.plugins`.
