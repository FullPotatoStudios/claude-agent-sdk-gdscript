# UI

Reusable UI scenes and supporting scripts belong here later.

The reusable chat panel is intentionally outside the core parity target and should only depend on the adapter layer, not directly on transport internals.
