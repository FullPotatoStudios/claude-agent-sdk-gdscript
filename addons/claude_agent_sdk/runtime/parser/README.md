# Parser

Phase 4 will implement the raw CLI message parser here.

The parser should:

- convert streamed Claude CLI JSON into typed runtime models
- skip unknown message types for forward compatibility
- stay scene-free
